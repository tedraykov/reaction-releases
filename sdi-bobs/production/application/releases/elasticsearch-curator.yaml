apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: elasticsearch-curator
  namespace: logging
spec:
  releaseName: elasticsearch-curator
  chart:
    repository: https://kubernetes-charts.storage.googleapis.com
    name: elasticsearch-curator
    version: "1.2.1"
  values:
    configMaps.config_yml: |-
      ---
      client:
        hosts:
          - vpc-production-logs-h5thmymtp327yrdyej2mi2cm4a.us-east-1.es.amazonaws.com
        port: 443
        use_ssl: True
        logging:
          loglevel: WARN
