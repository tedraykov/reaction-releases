apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kafka-topics
  namespace: kafka
spec:
  interval: 5m
  chart:
    spec:
      chart: "./charts/kafka-topics"
      sourceRef:
        kind: GitRepository
        name: bobs-helm-charts
        namespace: flux
      interval: 1m
  values:
    config:
      cluster: staging.k8s.bobs-test.io
