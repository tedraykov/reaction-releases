apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kafka-schema-registry
  namespace: dev
spec:
  interval: 5m
  chart:
    spec:
      chart: "./charts/cp-schema-registry"
      sourceRef:
        kind: GitRepository
        name: bobs-helm-charts
        namespace: flux
      interval: 1m
  values:
    fullnameOverride: kafka-schema-registry
    kafka:
      bootstrapServers: "PLAINTEXT://b-1.staging.x225n6.c8.kafka.us-east-1.amazonaws.com:9092,PLAINTEXT://b-2.staging.x225n6.c8.kafka.us-east-1.amazonaws.com:9092,PLAINTEXT://b-3.staging.x225n6.c8.kafka.us-east-1.amazonaws.com:9092"
