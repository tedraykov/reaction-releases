apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: bobs-helm-charts
  namespace: flux
spec:
  interval: 10m
  url: ssh://git@github.com/frasersgroup/reaction-releases.git
  ref:
    branch: gitops/bobs-dev-env
  secretRef:
    name: flux-sync
