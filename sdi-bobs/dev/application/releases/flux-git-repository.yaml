apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: bobs-helm-charts
  namespace: flux
spec:
  interval: 5m
  url: ssh://git@github.com:roberts50/reaction-releases.git
  ref:
    branch: gitops/bobs-dev-env
  secretRef:
    name: flux2-sync
