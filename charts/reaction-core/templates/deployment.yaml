{{- $fullName := include "reaction-core.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullName }}
  labels:
    app.kubernetes.io/name: {{ include "reaction-core.name" . }}
    app.kubernetes.io/component: core
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "reaction-core.chart" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "reaction-core.name" . }} 
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
{{- if .Values.annotations }}
      annotations:
        {{- with .Values.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
{{- end }}
      labels:
        app.kubernetes.io/name: {{ include "reaction-core.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "config" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "hydra" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "inventory" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "kafka" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "loyalty" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "orderexport" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "pricing" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "product" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "taxes" }}
            - configMapRef:
                name: {{ printf "%s-%s" $fullName "radial" }}
            - secretRef:
                name: reaction-core
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
