{{- $fullName := include "reaction-core.fullname" . -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $fullName }}-loyalty
  labels:
    app.kubernetes.io/name: {{ include "reaction-core.name" . }}
    app.kubernetes.io/component: core
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "reaction-core.chart" . }}
data:
  LOYALTY_PLUGIN_BATCH_CONSUME_SIZE: {{ required "loyalty.pluginBatchConsumeSize is not defined." .Values.loyalty.pluginBatchConsumeSize | quote }}
