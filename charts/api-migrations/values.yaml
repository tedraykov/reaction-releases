# Default values for api-migrations.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: 816897914983.dkr.ecr.us-east-1.amazonaws.com/reaction-api-migrations
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""
env: []
resources:
  requests:
    cpu: 1
    memory: "2Gi"
secretName: mongodb
